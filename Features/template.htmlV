<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{artist_name}} — Official AI Landing Page</title>

    <style>
        body {
            background: #000;
            color: #fff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .header {
            background: linear-gradient(90deg, #ff6600, #cc0000);
            padding: 40px;
            text-align: center;
        }

        .header h1 {
            font-size: 42px;
            margin: 0;
            font-weight: bold;
        }

        .section {
            padding: 30px;
            max-width: 900px;
            margin: auto;
        }

        .section h2 {
            margin-top: 0;
            border-bottom: 1px solid #444;
            padding-bottom: 10px;
        }

        .music-player {
            margin-top: 15px;
            padding: 20px;
            background: #111;
            border-radius: 8px;
            text-align: center;
        }

        .socials a {
            text-decoration: none;
            color: #ff6600;
            margin-right: 15px;
            font-weight: bold;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            padding: 20px;
            color: #666;
        }
    </style>
</head>

<body>

    <div class="header">
        <h1>{{artist_name}}</h1>
    </div>

    <div class="section">
        <h2>About</h2>
        <p>{{artist_bio}}</p>
    </div>

    <div class="section">
        <h2>Latest Release</h2>
        <div class="music-player">
            <p><strong>{{track_title}}</strong></p>
            <audio controls>
                <source src="{{track_url}}" type="audio/mpeg">
            </audio>
        </div>
    </div>

    <div class="section">
        <h2>Social Links</h2>
        <div class="socials">
            <a href="{{spotify}}" target="_blank">Spotify</a>
            <a href="{{youtube}}" target="_blank">YouTube</a>
            <a href="{{instagram}}" target="_blank">Instagram</a>
        </div>
    </div>

    <div class="footer">
        Powered by Flame Division • AI Artist Engine v1.0
    </div>

</body>
</html>
